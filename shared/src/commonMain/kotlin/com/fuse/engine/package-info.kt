package com.fuse.engine

/**
 * Engine layer — pure, deterministic game logic (board, tiles, moves, merge,
 * spawn, scoring, win/lose). No Compose, no platform, no DI framework leaking in.
 *
 * Empty placeholder for FND-3 (DI + module skeleton). Real logic lands in
 * Sprint 1 (ENG-*). The corresponding Koin module is
 * [com.fuse.di.engineModule].
 */
internal object EnginePackage
