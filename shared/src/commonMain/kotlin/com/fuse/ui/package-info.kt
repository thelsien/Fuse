package com.fuse.ui

/**
 * UI layer — Compose Multiplatform composables + design tokens (theme, colors,
 * type, spacing). Consumes the presentation layer; design tokens land in FND-4.
 *
 * Empty placeholder for FND-3 (DI + module skeleton). The Koin module is
 * [com.fuse.di.uiModule]; the sample dependency is surfaced by the root
 * composable in com.fuse.shared.App.
 */
internal object UiPackage
