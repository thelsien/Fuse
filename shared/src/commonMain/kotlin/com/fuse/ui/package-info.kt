package com.fuse.ui

/**
 * UI layer — Compose Multiplatform composables + design tokens.
 *
 * The design-token / theme layer lives in [com.fuse.ui.theme] (FND-4): colors,
 * typography, spacing, shapes, motion, the `FuseTheme` composable + Composition
 * locals, and the swatch preview screen. The Koin module is [com.fuse.di.uiModule]
 * (reserved for Koin-scoped UI services, not the raw tokens).
 */
internal object UiPackage
